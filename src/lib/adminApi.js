import { supabase } from './supabaseClient'

export async function runAdminAction(payload){const {data,error}=await supabase.functions.invoke('admin-account',{body:payload});if(error)throw error;return data}
export async function loadTeam(){const [{data:profiles,error:profileError},{data:presence,error:presenceError},{data:slots,error:slotError}]=await Promise.all([supabase.from('zoom_profiles').select('id,username,initials,role,status,avatar_id,created_at').order('created_at'),supabase.from('zoom_presence').select('user_id,state,last_heartbeat'),supabase.from('zoom_agent_slots').select('id,initials,status,claimed_by,created_at').order('created_at')]);if(profileError)throw profileError;if(presenceError)throw presenceError;if(slotError)throw slotError;const states=new Map((presence||[]).map(item=>[item.user_id,item.state]));const claimed=new Set((slots||[]).filter(slot=>slot.claimed_by).map(slot=>slot.claimed_by));return [...(profiles||[]).map(person=>({...person,presence:states.get(person.id)||'offline'})),...(slots||[]).filter(slot=>slot.status==='pending'&&!claimed.has(slot.claimed_by)).map(slot=>({id:`pending-${slot.id}`,initials:slot.initials,status:'pending',pending:true,presence:'offline',role:'agent'}))]}
export async function loadUsage(){const [{data:events,error:eventError},{data:presence,error:presenceError}]=await Promise.all([supabase.from('zoom_usage_events').select('event_type,created_at,user_id').order('created_at',{ascending:false}).limit(200),supabase.from('zoom_presence').select('user_id,state,last_heartbeat')]);if(eventError)throw eventError;if(presenceError)throw presenceError;return {events:events||[],presence:presence||[]}}
export async function loadFeedback(){const {data,error}=await supabase.from('zoom_feedback_reports').select('id,type,status,page_label,process_id,what_noticed,suggested_change,created_at,reporter_user_id').order('created_at',{ascending:false});if(error)throw error;return data||[]}
