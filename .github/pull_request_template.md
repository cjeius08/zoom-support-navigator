## What changed

Briefly describe the Ozzie change.

## What’s New audience check

- [ ] **User-facing change:** I added or updated an `audience: "all"` entry in `src/features/updates/updatesData.json`.
- [ ] **Admin-only operational/admin change:** I added or updated an `audience: "admin"` entry when it is useful for administrators.
- [ ] **Developer-only change:** No What’s New entry is needed because this does not change how Ozzie is used or operated.

Only check the option that applies. Do not add developer noise such as refactors, test-only changes, commit details, backup branches, or deployment mechanics unless they materially affect Ozzie users or administrators.

## Verification

- [ ] Relevant tests pass.
- [ ] Lint passes.
- [ ] Production build passes.
